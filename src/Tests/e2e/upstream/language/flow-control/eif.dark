(if true then "correct" else 0L) = error="Type mismatch in if branches must have same type: expected String, got Int64"

(if true then Builtin.testRuntimeError "a" else 0L) = exit=1 stderr="Uncaught exception: a"

(if false then 0L else Builtin.testRuntimeError "a") = skip="Builtin.testRuntimeError is not supported yet"

(if false then Builtin.testRuntimeError "a" else 0L) = skip="Builtin.testRuntimeError is not supported yet"
(if true then 0L else Builtin.testRuntimeError "a") = skip="Builtin.testRuntimeError is not supported yet"
(if false then "" else "correct") = "correct"
(if () then "" else "") = error="Encountered a condition that must be a Bool, but got an Unit (())"

(if Builtin.testRuntimeError "msg" then "" else "") = skip="Builtin.testRuntimeError is not supported yet"

(if 5L then "" else "") = error="Encountered a condition that must be a Bool, but got an Int64 (5)"

(if true then ()) = ()
(if 1L > 3L then 4L) = error="Type mismatch in if branches must have same type: expected Int64, got Unit"
(if 1L < 3L then 4L) = error="Type mismatch in if branches must have same type: expected Int64, got Unit"
(if 1L < 3L then "msg") = error="Type mismatch in if branches must have same type: expected String, got Unit"

(if () then "msg") = error="Encountered a condition that must be a Bool, but got an Unit (())"

(if true then Builtin.testRuntimeError "a") = skip="Builtin.testRuntimeError is not supported yet"

(if Builtin.testRuntimeError "msg" then "") = skip="Builtin.testRuntimeError is not supported yet"
